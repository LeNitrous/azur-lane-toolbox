name: CI
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy to GitHub Pages
      run: |
        chmod +x ./deploy.sh
        ./deploy.sh
